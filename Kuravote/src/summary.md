Each sub-folder under the src folder contains components that are unique to it

The assets folder: contains images, SVGs, logo, etc.

The components folder: contains reusable UI components

The pages folder: contains main page screens per workflow/role

The utils folder: contains helpers (api.js, validators, etc.)

The routes folder: contains the auth routing logic
